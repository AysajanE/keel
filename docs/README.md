# Keel Docs

Start here:

- `quickstart.md` for first local checks
- `concepts.md` for the contract and boundaries
- `tools.md` for the manifest-owned tool map
- `integrations/gbrain.md` for the optional memory integration

Architecture notes live under `architecture/`. The HTML walkthrough is
`diagrams/keel-explained.html`.
