# Architecture Notes

These notes explain the main Keel design decisions:

- keep tools separate
- pass reviewed artifacts through `markdown_playbook_v1`
- keep human gates human-controlled
- avoid vendoring tool source into product repositories

They are maintained as public design references, not raw local review logs.
