# Hello World Fixture

This fixture is a small product-repo-shaped directory for testing Keel docs and
wrapper examples.

It is intentionally tiny and does not require API keys.

```bash
export PRODUCT_REPO="$HOME/keel/examples/hello-world"
keel-smoke
```

Use it with the commands in `docs/quickstart.md`.
